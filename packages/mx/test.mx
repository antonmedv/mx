section className="main"
  if todos.length > 0
    input className="toggle-all" type="checkbox" checked={completedCount === todos.length} onChange={actions.completeAll}
  ul className="main"
    if todo in filteredTodos
      TodoItem key={todo.id} todo={todo}
    else if toto.f > 1
      p Okay
  if todos.length
    Footer completedCount={completedCount} activeCount={activeCount} filter={filter} onClearCompleted={actions.clearCompleted} onShow={handleShow}
  div Муц!
